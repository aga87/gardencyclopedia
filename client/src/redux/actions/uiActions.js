import { OPEN_PLANT_MODAL, CLOSE_PLANT_MODAL } from './types';

export const openPlantModal = () => ({
  type: OPEN_PLANT_MODAL
});

export const closePlantModal = () => ({
  type: CLOSE_PLANT_MODAL
});
